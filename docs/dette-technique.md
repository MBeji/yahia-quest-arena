# Dette technique — ce qui reste vraiment ouvert

> **Re-vérifié dans le code le 2026-08-26** (`main` à #884) — la passe précédente datait du
> 2026-08-02 (#703), et l'écart entre les deux est la meilleure démonstration de la règle
> ci-dessous : trois des quatre lignes « ouvertes » citaient un chiffre que le code avait
> démenti. Ce fichier remplace le plan d'action
> de l'audit du 2026-06-30, dont la moitié des items « ouverts » étaient soldés sans que
> personne ne coche la case — c'est précisément le genre de rot qu'un backlog non vérifié
> produit. **Règle** : on n'inscrit ici qu'une dette dont on a re-lu la ligne de code, et on
> la sort quand la ligne a changé, pas quand on croit s'en souvenir.
>
> L'audit d'origine est archivé : [`docs/archive/2026-06-30-audit-codebase.md`](./archive/2026-06-30-audit-codebase.md)
> (+ ses métriques `.csv`). Les axes **perf** ont leur propre document vivant,
> [`docs/performance-audit.md`](./performance-audit.md), et son harnais de charge
> [`perf/README.md`](../perf/README.md).

## Ouvert

> **Re-mesuré le 2026-08-26** (`main` à #884), ligne de code par ligne de code, par une passe
> qualité à 6 axes. Ce que la re-mesure a trouvé mérite d'être dit, parce que c'est exactement
> ce que l'en-tête ci-dessus interdit : **trois des quatre lignes citaient des chiffres que le
> code avait démentis**. Le compteur de `quest.server.ts` datait de douze jours et sous-estimait
> de 22 lignes ; surtout, il désignait le mauvais fichier — deux autres sont plus gros.

| Dette                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | Où                                            | Effort | Axe               |
| ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------- | ------ | ----------------- |
| **Trois gros fichiers, et le lint est à deux lignes de trancher pour nous.** `dashboard.server.ts` **1 084 l.**, `tutor.server.ts` **1 051 l.**, `quest.server.ts` **1 005 l.** (mesuré le 2026-08-26 ; cette ligne annonçait 983 pour le seul dernier). Le gate qui compte vraiment est `max-lines: 750` **en lignes de code** (`eslint.config.js`, `skipBlankLines` + `skipComments`) : cinq fichiers sont à moins de 30 lignes du plafond — `dashboard.test.ts` 749, `exercise-player.tsx` 743, `dashboard.server.ts` 739, `quest.server.ts` 726, `tutor.server.ts` 724. Le prochain lot d'é30 ou d'é11 rougira le lint pour une raison **sans rapport avec son diff**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | `src/features/{dashboard,tutor,quest}/`       | M      | 📝 Qualité        |
| **Pas de _budget_ de perf runtime** — le LCP réel est **mesuré** (RUM `web-vitals.ts` → PostHog) et les server fns lentes journalisées (≥ 1 s), mais rien ne **bloque** une régression : le budget **bundle** existe (`build:check`) et le harnais de charge aussi (`perf:check`, réparé le 2026-08-26 — il ne pouvait pas tourner sur un poste Windows), pas de gate sur le LCP                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | CI                                            | M      | 🧪 Tests          |
| **Un appel IA tué par la plateforme débite l'énergie et ne la rend jamais.** Re-vérifié en SQL le 2026-08-26 et **exact** : `sweep_ai_reservations()` (migration `20260823110000`) balaie `ai_spend_ledger` et `ai_platform_ledger` — **pas `ai_energy_ledger`**, qui n'a d'ailleurs pas de colonne `reserved`. L'argent s'auto-répare après 5 min, l'énergie de l'élève non. Mesuré en prod le 2026-08-25 : deux générations parties, énergie 0/18 → **6/18**, console de dépense figée sur « 1 appel · 0,00 $ »                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | `src/features/ai/ai-call.server.ts` + SQL     | M      | 📝 Qualité        |
| **Les vues d'ombre RPC restantes — et le motif qui les reproduit.** Faits : `exam` (#889), les 7 de `ai/` (#900), les 5 de `tutor/`. **Restent 10 vues à cast** — `dashboard/` (7), `progression/` (2), `quest/` (1) — plus **2 cas à part** (`shared/lib/rate-limit.ts`, `shared/integrations/ai/usage.server.ts`) qui ne sont PAS des casts : la vue y sert de type de **paramètre**, c'est de l'inversion de dépendance, et leur sort demande son propre arbitrage (testabilité contre vérification des arguments). ⚠️ **La source coule encore** : `ai-vault.server.ts`, créé le 2026-08-28 (#899), portait une vue neuve deux jours après la régénération qui la rendait inutile — une règle ESLint `local/` refusant `as unknown as *Rpc*` fermera la porte **une fois les 10 dernières tombées**. Trois classes, jamais à confondre : **(A)** paramètre `DEFAULT NULL` → `?? undefined`, **zéro cast** ; **(B)** paramètre obligatoire dont la colonne est nullable → `nullableRpcArg()` ; **(C)** le SQL REFUSE NULL et le code a déjà une garde que TypeScript ne sait pas lire → **rendre la garde prouvable**, surtout pas élargir le type (vu sur `get_tutor_parent_digest.p_student_id`). **Lire la signature SQL avant de choisir** | `src/features/{dashboard,progression,quest}/` | M      | 📝 Qualité        |
| **`maxDuration: 300` est recopié à trois endroits, jamais partagé.** Le littéral vit dans `scripts/build-vercel.mjs:122`, il est re-déclaré en dur dans `src/shared/integrations/ai/__tests__/openai-compatible.test.ts` (`SSR_MAX_DURATION_MS`), et `scripts/ai/tutor-digests.mjs` s'y aligne à la main. C'est exactement l'invariant maintenu à la main qui vient de dériver : le délai client des bilans est resté calé sur un `maxDuration: 30` mort pendant que le serveur passait à 300 (corrigé le 2026-08-26, avec cinq commentaires qui disaient encore « trente secondes »). Le geste qui ferme le trou : une constante unique, lue par les trois                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | `scripts/`, `src/shared/integrations/ai/`     | S      | 📝 Qualité        |
| **L'environnement jsdom est global : 81 % du temps mural de la suite.** Run complet mesuré ici : `Duration 516 s` dont **`environment 486 s`** ; 178 des 276 fichiers de test n'ont aucun usage du DOM (les 31 de `scripts/**` en premier). C'est le premier levier sur le gate local — celui-là même dont la contention Windows a fait échouer un `git push` le 2026-08-26 (#879). Vitest 4 n'a plus `environmentMatchGlobs` : le geste est `test.projects` (un projet `node`, un projet `jsdom`), et une mauvaise classification échoue **bruyamment**, jamais en silence                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | `vitest.config.ts`                            | M      | 🧪 Tests          |
| **34 des 55 primitives `components/ui` n'ont aucun importeur** — ~3 400 lignes, et une vingtaine de dépendances npm qui n'existent que pour elles. À part : `date-fns`, `lenis` et `@hookform/resolvers` n'ont **zéro référence** dans tout le dépôt (`src`, `scripts`, `e2e`, `perf`, configs). Lot purement soustractif, prouvé par `tsc` + `build:check` ; il touche le lockfile, donc à livrer quand la CI peut le valider                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | `src/components/ui/`, `package.json`          | M      | 🧹 Simplification |
| **« Une feature n'importe jamais une autre » n'a aucun gate, et la règle est violée 5 fois** — dont trois par la couche IA (`ai`/`exam`/`tutor`), c'est-à-dire par le code que la roadmap est en train d'allumer. Trancher d'abord (`ai/` est-elle une feature ou un service partagé ?), poser le gate ensuite : dans l'autre ordre il naît rouge                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | `eslint.config.js`, `src/features/`           | M      | 📝 Qualité        |

## Latent — réveillé seulement par un retour du premium

| Dette                                                                                                                                                                                                                                                                              | Où                                                     |
| ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------ |
| **N+1 sur `has_parcours_entitlement`** — un appel RPC par parcours. **Ne se déclenche pas aujourd'hui** : les deux sites d'appel court-circuitent sur `!p.is_premium`, et la phase gratuite met `is_premium = false` partout. À batcher **avant** tout dégel du premium (étude 01) | `src/features/dashboard/dashboard.server.ts:323, :769` |

## Soldé depuis l'audit (2026-06-30 → 2026-08-13)

- **`npm ci` cassé sur `main`** — diagnostiqué ici le 2026-08-10 (#716 avait retiré du lock le
  `typescript@5.9.3` imbriqué dont `tsconfck` a besoin ; npm 10 mourait en `EUSAGE`), **corrigé
  sur `main` le 2026-08-10 par le revert #718**, qui cite la même erreur — la cause profonde
  était que le bump undici embarquait un miniflare 5 alpha. Reste vrai, et sans gravité : dev+CI
  tournent en **Node 24** (`.nvmrc`), la fonction SSR en prod en **`nodejs22.x`**
  (`build-vercel.mjs`) — deux chiffres qui cohabitent légitimement.

- **Élagage push ligne à ligne** — soldé le 2026-08-10 : les endpoints morts sont
  collectés puis supprimés en **un** `DELETE … IN (…)` par lot de 200 (l'`.in()`
  voyage dans l'URL, qui a un plafond). L'élagage est best-effort — les notifications
  sont déjà parties, un nettoyage raté ne fait plus échouer le cron.
  `notifications.cron.server.ts` ; deux tests épinglent le comportement (un seul
  aller-retour pour 5 endpoints morts, et `pruned: 0` si la suppression échoue).
- **Chunks vendor sans budget** (M1-fe de l'audit perf) — soldé le 2026-08-10 :
  `vendor-radix`, `vendor-icons` et `vendor-three` ont désormais un plafond.

Consigné parce que le plan d'action les portait encore comme ouverts :

- **Agrégats `subject_stats`** — le fetch non borné des `attempts` dans `getDashboard` a été
  remplacé par la RPC `get_user_subject_stats` (`GROUP BY subject_id`, une ligne bornée par
  matière). `dashboard.server.ts:238`.
- **Trous de tests** — `recoverStreak` est couvert (`progression/__tests__/progression.test.ts`),
  `handlePushCron` aussi (`notifications/__tests__/notifications.cron.test.ts`), et
  `content-protection.ts` a le sien (`shared/lib/__tests__/content-protection.test.ts`).
- **Quick wins de la PR #245** — redaction du logger étendue, `npm audit fix`,
  `no-unused-vars` réactivé, tag SemVer outillé (`release.yml`).
