# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
.output
.vinxi
.tanstack/**
.nitro
*.local

# Playwright (e2e)
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/

# Wrangler / Cloudflare
.wrangler/
.dev.vars

# Environment (secrets)
.env
.env.*
# …but keep the committed, secret-free TEST template:
!.env.test.example
.sbtoken

# Claude Code — share settings.json, hooks & skills; keep personal overrides local
.claude/settings.local.json
# Ephemeral git worktrees created for background agents (never commit these)
.claude/worktrees/

# Supabase local
supabase/.temp/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.vercel

# Coverage reports
coverage/

# Audit reports (generated)
AUDIT_REPORT*
audit-actions-checklist*
audit-metrics*

# Playwright authenticated storage states (generated by the setup project)
e2e/.auth/
bun.lock
